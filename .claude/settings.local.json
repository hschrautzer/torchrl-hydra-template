{
  "permissions": {
    "allow": [
      "Bash(timeout 120 uv run python -m src.train experiment=dqn/atari_pong trainer.total_frames=100 trainer.log_interval=50 trainer.save_trainer_interval=100 trainer.optim_steps_per_batch=1 trainer.progress_bar=true collector.init_random_frames=20 collector.frames_per_batch=20 replay_buffer.batch_size=4 replay_buffer.storage.max_size=200 trainer.annealing_num_steps=50 hydra.run.dir=/tmp/torchrl-atari-smoke)"
    ]
  }
}
