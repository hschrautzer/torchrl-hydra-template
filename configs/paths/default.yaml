# path configuration (following lightning-hydra-template convention)
# all paths are relative to the project root

root_dir: ${hydra:runtime.cwd}
log_dir: ${paths.root_dir}/logs/
output_dir: ${hydra:runtime.output_dir}/
